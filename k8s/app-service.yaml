apiVersion: v1
kind: Service
metadata:
  name: sales-agent-service
  labels:
    app: sales-agent
    tier: api
spec:
  type: ClusterIP
  ports:
    - port: 8000
      targetPort: 8000
      name: http
  selector:
    app: sales-agent
    tier: api
