apiVersion: v1
kind: Service
metadata:
  name: redis-service
  labels:
    app: redis
    component: session-store
spec:
  type: ClusterIP
  ports:
    - port: 6379
      targetPort: 6379
      name: redis
  selector:
    app: redis
